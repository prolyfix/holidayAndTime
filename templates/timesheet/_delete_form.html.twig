<form method="post" action="{{ path('app_timesheet_delete', {'id': timesheet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ timesheet.id) }}">
    <button class="btn">Delete</button>
</form>
