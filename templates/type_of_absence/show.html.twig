{% extends 'base.html.twig' %}

{% block title %}TypeOfAbsence{% endblock %}

{% block body %}
    <h1>TypeOfAbsence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_absence.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_of_absence.name }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ type_of_absence.creationDate ? type_of_absence.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IsHoliday</th>
                <td>{{ type_of_absence.isHoliday ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_absence_index') }}">back to list</a>

    <a href="{{ path('app_type_of_absence_edit', {'id': type_of_absence.id}) }}">edit</a>

    {{ include('type_of_absence/_delete_form.html.twig') }}
{% endblock %}
