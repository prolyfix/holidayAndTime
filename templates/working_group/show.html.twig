{% extends 'base.html.twig' %}

{% block title %}WorkingGroup{% endblock %}

{% block body %}
    <h1>WorkingGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ working_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ working_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_working_group_index') }}">back to list</a>

    <a href="{{ path('app_working_group_edit', {'id': working_group.id}) }}">edit</a>

    {{ include('working_group/_delete_form.html.twig') }}
{% endblock %}
