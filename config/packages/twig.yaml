when@test:
    twig:
        strict_variables: true  
twig:
    paths:
        '%kernel.project_dir%/src/Prolyfix/RssBundle/templates': 'RssBundle'
        '%kernel.project_dir%/src/Prolyfix/NoteBundle/templates': 'NoteBundle'
